import palette from './palette';

const primary = palette.primary;

export default {
  siderWraper: {
    background: '#F0F2F5',
    position: 'fixed',
    right: 0,
  },
  sider: {
    backgroundColor: primary, //'#ff0045',
  },
};
